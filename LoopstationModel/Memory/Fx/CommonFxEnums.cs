namespace Loopstation.Model.Memory.Fx
{
    // TODO: Rate [MIX] (0...100), {rate}
    public enum Rate { }
    // TODO: StepRate [MIX] (OFF...101), {step_rate}
    public enum StepRate { }

    // TODO: DelayTime [MIX] (0...1000), {delay_time}
    public enum DelayTime { }
}
